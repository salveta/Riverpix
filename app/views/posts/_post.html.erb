<div>
  <h2><%= link_to post.title, post_path(post) %></h2>
 
  <small><em><%= post.timestamp %></em></small>
  <p><img src="<%= post.avatar.url %>" />
  <p><%= truncate(strip_tags(post.body), length: 600) %></p>
</div>